{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Installer les dépendances frontend et backend",
			"type": "shell",
			"command": "npm install && cd backend && npm install",
			"group": "build"
		}
	]
}